# Copy this file to `.env` and set the path to your Ableton Live Beta install
# (the build that supports Extensions). Used by `npm start` / `extensions-cli run`.
EXTENSION_HOST_PATH=/Applications/Ableton Live 12 Beta.app
